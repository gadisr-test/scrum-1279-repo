const request = require('supertest');\nconst app = require('../../app');\n\ndescribe('Test Server Startup', () => {\n  it('should start the server without errors', async () => {\n    const response = await request(app).get('/');\n    expect(response.status).toBe(200);\n  });\n});\n\ndescribe('CRUD Operations for Time Zones', () => {\n  it('creates a new timezone successfully', async () => {\n    const response = await request(app).post('/timezones').send({ timezoneId: 'tz1' });\n    expect(response.status).toBe(200);\n    expect(response.body.message).toEqual('Timezone added');\n  });\n});